// App.jsx — Root component shell
// ThemeProvider and layout components will be added in Phase 1.
// This is a clean placeholder to verify Phase 0 setup.

function App() {
  return (
    <div style={{ color: 'var(--text)', padding: '2rem', fontFamily: 'DM Sans, sans-serif' }}>
      <h1 style={{ fontFamily: 'Syne, sans-serif', fontSize: '2rem', color: 'var(--accent)' }}>
        Portfolio V2 — Phase 0 ✓
      </h1>
      <p style={{ marginTop: '0.5rem', color: 'var(--muted)' }}>
        Vite + React + Tailwind CSS ready. Proceeding to Phase 1 (Theme System).
      </p>
    </div>
  )
}

export default App
